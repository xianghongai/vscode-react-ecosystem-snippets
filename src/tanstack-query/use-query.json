{
  "TanStack Query useQuery display (JavaScript)": {
    "prefix": "useQuery",
    "body": [
      "export function ${1:UserDetails}({ ${2:userId} }) {",
      "  const ${3:userQuery} = useQuery(${4:userQueryOptions}(${2}));",
      "",
      "  if (${3}.isPending) {",
      "    return <p>Loading…</p>;",
      "  }",
      "",
      "  if (${3}.isError) {",
      "    const message = ${3}.error instanceof Error ? ${3}.error.message : 'Unable to load data';",
      "    return <p role=\"alert\">{message}</p>;",
      "  }",
      "",
      "  return (",
      "    <article>",
      "      <h1>{${3}.data.name}</h1>",
      "      $0",
      "    </article>",
      "  );",
      "}"
    ],
    "description": "Render a query with its pending and error states. Requires useQuery from @tanstack/react-query and a queryOptions factory.\n\n渲染查询及其等待与错误状态；需要 @tanstack/react-query 的 useQuery，以及一个 queryOptions 工厂函数。",
    "scope": "javascriptreact"
  },
  "TanStack Query useQuery display (TypeScript)": {
    "prefix": "useQuery",
    "body": [
      "export function ${1:UserDetails}({ ${2:userId} }: { ${2}: string }) {",
      "  const ${3:userQuery} = useQuery(${4:userQueryOptions}(${2}));",
      "",
      "  if (${3}.isPending) {",
      "    return <p>Loading…</p>;",
      "  }",
      "",
      "  if (${3}.isError) {",
      "    const message = ${3}.error instanceof Error ? ${3}.error.message : 'Unable to load data';",
      "    return <p role=\"alert\">{message}</p>;",
      "  }",
      "",
      "  return (",
      "    <article>",
      "      <h1>{${3}.data.name}</h1>",
      "      $0",
      "    </article>",
      "  );",
      "}"
    ],
    "description": "Render a typed query with its pending and error states. Requires useQuery from @tanstack/react-query and a queryOptions factory.\n\n渲染类型化查询及其等待与错误状态；需要 @tanstack/react-query 的 useQuery，以及一个 queryOptions 工厂函数。",
    "scope": "typescriptreact"
  }
}
